# fableins.com deployment config
# Cloudflare Pages 自动读取，构建时不用这个文件
# 这里仅作记录

name = "fableins"
compatibility_date = "2025-01-01"
pages_build_output_dir = ".next"

# 自定义域名（在 Cloudflare Dashboard 配置）
# - fableins.com
# - www.fableins.com
